# Greenhouse fixtures

These fixtures are shared by TypeScript and Python tests for environmental
telemetry and greenhouse configuration. JSON cannot represent NaN or infinity,
so those malformed numeric cases are covered in language-specific tests.
